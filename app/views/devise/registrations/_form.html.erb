<div class="cart-recap">
  <div class="cart-content-header">
    <p>Mon compte</p>
  </div>
  <button type="button" data-dismiss="modal" class="btn btn-dismiss"><i class="fa fa-chevron-left" data-toggle="modal" data-target="#mycart"></i></button>
</div>
<div class="signup-wrapper">

  <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
      <%= link_to omniauth_authorize_path(resource_name, provider) do %>
        <div class="social text-center">
          <div class="btn-f btn-large-f btn-facebook"><i class="fa fa-facebook"></i> S'inscrire avec Facebook
          </div>
        </div>
      <% end %>
          <div class="promis">Nous ne publierons jamais rien sans votre autorisation.
          </div>
        <h3 class="text-center">ou</h3>
    <% end -%>
  <% end -%>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>
      <%= f.input :password, label: "Mot de passe", required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
      <%= f.input :password_confirmation, label: "Confirmation du mot de passe", required: true %>
    </div>
    <div class="form-actions text-center">
      <%= f.button :submit, "S'inscrire", class: "btn-primary" %>
    </div>
  <% end %>

        <div class="forgotten">
  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%= link_to new_password_path(resource_name) do %>
          <p>Mot de passe oublié ?</p>
        </div>
      <% end %>
    <br/>
  <% end -%>
<!--   %- if controller_name != 'sessions' %>
  %= link_to "Se connecter", new_session_path(resource_name) %><br />
  % end -%> -->

</div>
